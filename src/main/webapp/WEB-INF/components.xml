<?xml version="1.0" encoding="UTF-8"?>
<components xmlns="http://jboss.com/products/seam/components"
            xmlns:core="http://jboss.com/products/seam/core"
            xmlns:persistence="http://jboss.com/products/seam/persistence"
            xmlns:drools="http://jboss.com/products/seam/drools"
            xmlns:bpm="http://jboss.com/products/seam/bpm"
            xmlns:security="http://jboss.com/products/seam/security"
            xmlns:mail="http://jboss.com/products/seam/mail"
            xmlns:web="http://jboss.com/products/seam/web"
            xmlns:transaction="http://jboss.com/products/seam/transaction"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation=
                "http://jboss.com/products/seam/core http://jboss.com/products/seam/core-2.1.xsd
                 http://jboss.com/products/seam/persistence http://jboss.com/products/seam/persistence-2.1.xsd
                 http://jboss.com/products/seam/drools http://jboss.com/products/seam/drools-2.1.xsd
                 http://jboss.com/products/seam/bpm http://jboss.com/products/seam/bpm-2.1.xsd
                 http://jboss.com/products/seam/security http://jboss.com/products/seam/security-2.1.xsd
                 http://jboss.com/products/seam/mail http://jboss.com/products/seam/mail-2.1.xsd
                 http://jboss.com/products/seam/web http://jboss.com/products/seam/web-2.1.xsd
                 http://jboss.com/products/seam/transaction http://jboss.com/products/seam/transaction-2.1.xsd
                 http://jboss.com/products/seam/components http://jboss.com/products/seam/components-2.1.xsd">

   <!-- false para PRODUCAO -->
   <core:init debug="true" jndi-pattern="@jndiPattern@"/>
   
   <!-- Variaveis globais de contexto da aplicacao -->
   <factory name="contextPath" scope="APPLICATION" value="#{facesContext.externalContext.request.contextPath}" auto-create="true"/>
   <factory name="basePath"    scope="APPLICATION" value="#{facesContext.externalContext.request.scheme}://#{facesContext.externalContext.request.serverName}:#{facesContext.externalContext.request.serverPort}#{contextPath}/" auto-create="true"/>

   <!-- Mail Server -->
   <mail:mail-session host="192.168.0.74" port="25" tls="true" ssl="false" username="app.ouvidoria" password="ouvi159doria"/>
	
   <component name="org.jboss.seam.debug.hotDeployFilter">
      <property name="urlPattern">*.seam</property>
   </component>

   <core:manager concurrent-request-timeout="500"
                 conversation-timeout="120000"
                 conversation-id-parameter="cid"
                 parent-conversation-id-parameter="pid"/>


   <!-- Make sure this URL pattern is the same as that used by the Faces Servlet -->
   <web:rewrite-filter view-mapping="*.seam" />
   <web:hot-deploy-filter url-pattern="*.seam"/>
   <web:redirect-filter url-pattern="*.seam" />
   <web:character-encoding-filter encoding="UTF-8" override-client="true" url-pattern="*.seam" />
   
   <!-- Definicoes do File Upload -->	
   <web:multipart-filter create-temp-files="true"
                         max-request-size="20000000" 
                         url-pattern="*.seam" />

   <persistence:entity-manager-factory name="entityManagerFactory" persistence-unit-name="sofistcarros"/>

   <persistence:managed-persistence-context name="entityManager" auto-create="true" entity-manager-factory="#{entityManagerFactory}"/>

   <!-- TOMCAT -->
   <!--<transaction:entity-transaction entity-manager="#{entityManager}"/>  -->
   
   <!-- Captura a sessao do JPA para injetar no Hibernate -->
   <factory auto-create="true" name="session" scope="stateless" value="#{entityManager.delegate}" />
   
   <drools:rule-base name="securityRules">
      <drools:rule-files><value>/security.drl</value></drools:rule-files>
   </drools:rule-base>

   <security:rule-based-permission-resolver security-rules="#{securityRules}"/>

   <security:identity authenticate-method="#{authenticator.authenticate}" remember-me="true"/>


   <event type="org.jboss.seam.security.notLoggedIn">
      <action execute="#{redirect.captureCurrentView}"/>
   </event>
   
   <event type="org.jboss.seam.security.loginSuccessful">
    	<action execute="#{authenticator.carregaUsuario}"/>
    	<action execute="#{authenticator.isPrimeiroAcesso}"/>
    	<action execute="#{redirect.returnToCapturedView}" />
	</event>

   <!-- For use with jBPM pageflow or process management -->
   <!--
   <bpm:jbpm>
      <bpm:process-definitions></bpm:process-definitions>
      <bpm:pageflow-definitions></bpm:pageflow-definitions>
   </bpm:jbpm>
   -->

</components>
